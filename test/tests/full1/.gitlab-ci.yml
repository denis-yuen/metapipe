test full1:
  stage: test
  tags:
    - test-server
    - bash

  script:
    - mkdir -p target && cd target
    - nextflow run ..
    #- docker run --rm -v ..:/workflow -v /var/run/docker.sock:/var/run/docker.sock nextflow/nextflow:20.04.1 nextflow run /workflow
